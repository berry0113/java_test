継承する必要がない
1つのにじさんじクラスを作ればnewを使ってデータを組み込めば完成
16章のデータ格納を使用するのが理想
空のインターフェースを作って実装すればいい
名前と詳細情報を持つクラスを作りそれを継承してグループ名を加えたにじさんじクラスを作る
先にクラスとメソッドづくり実行するメインクラスが一番最後


例外は入力された値で想定されていなさそうなものを入れる

カプセル化は外部から情報を変えられないようにするので
メンバー名か詳細の情報に使う

インターフェースは機能を作るので閲覧となにかつくる

継承はグループを選択する⇒メンバー表示⇒細かく表示
でそれぞれの機能を上から順に継承していく


